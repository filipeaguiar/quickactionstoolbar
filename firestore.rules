rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Secure default until the owner/member rules are implemented and covered
    // by Firebase Emulator tests.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
