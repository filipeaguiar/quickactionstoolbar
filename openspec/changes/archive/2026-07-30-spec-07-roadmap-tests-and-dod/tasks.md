## 1. Integration & DoD Tests

- [x] 1.1 Create `tests/dod.test.ts` to test end-to-end flow (Profile -> Action -> System Pack transform -> Dice+ Adapter payload formatting)
- [x] 1.2 Verify all criteria from the Definition of Done matrix in test suite (`npm test`)

## 2. Final Build Verification

- [x] 2.1 Run production build (`npm run build`) and confirm zero TypeScript and Vite bundle errors
