{
  "name": "quick-actions-toolbar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run icons:check",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "firebase:emulators": "firebase emulators:start",
    "firebase:emulators:exec": "firebase emulators:exec --only auth,firestore,hosting",
    "test:rules": "firebase emulators:exec --only firestore --project quickactionsbar \"vitest run --config vitest.rules.config.ts\"",
    "firebase:login": "firebase login",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy preview --expires 7d --project default",
    "deploy:production": "npm run build && firebase deploy --only hosting,firestore:rules,firestore:indexes --project production",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes --project production",
    "icons:generate": "node scripts/generate-rpg-awesome-icons.mjs",
    "icons:check": "node scripts/generate-rpg-awesome-icons.mjs --check"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "^3.1.0",
    "firebase": "^12.17.0",
    "rpg-awesome": "^0.2.0",
    "vue": "^3.4.21",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^20.11.24",
    "@vitejs/plugin-vue": "^5.0.4",
    "firebase-tools": "^15.25.1",
    "typescript": "~5.4.5",
    "vite": "^5.1.4",
    "vitest": "^1.3.1",
    "vue-tsc": "^2.2.12"
  }
}
